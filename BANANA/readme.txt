Installation

Les .jars sont générés avec la commande "ant", les jars que nous avons généré avant le rendu se trouve dans le répertoire jar.

Utilisation

Le server se lance avec la commande "java -jar serverTCP.jar port"
Le client se lance avec la commande "java -jar clientTCP.jar IP port"
Il est conseillé de lancer le client avec rlwrap avec la commande "rlwrap java -jar clientTCP.jar IP port" ce qui permet
de retrouver ce que l'on a écrit en appuyant sur la fleche haut du clavier.

Les documents demandé pour le rendu se trouve dans le répertoire docs.
